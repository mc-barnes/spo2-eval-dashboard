export default function RPM() {
  return (
    <main className="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
      <p className="text-xs uppercase tracking-wider text-muted mb-3">
        Business · Pediatric home monitoring framing
      </p>
      <h1 className="font-heading text-3xl sm:text-4xl font-semibold text-teal-dark leading-tight mb-4">
        Remote patient monitoring
      </h1>
      <p className="font-body text-body leading-relaxed">
        Operational infrastructure for pediatric home monitoring as a category — with
        adult RPM as a generalization, not the primary frame. Full four-panel value
        story lands in a follow-up ticket; this route exists today so the IA is testable.
      </p>
    </main>
  );
}
